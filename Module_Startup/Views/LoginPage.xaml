<Page
    x:Class="MTM_Waitlist.Module_Startup.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="32">
        <StackPanel
            MaxWidth="460"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="16">
            <TextBlock
                FontSize="28"
                FontWeight="SemiBold"
                Text="Sign in" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="{x:Bind ViewModel.LoginHint, Mode=OneWay}"
                TextWrapping="WrapWholeWords" />
            <StackPanel
                x:Name="NewUserActionArea"
                Orientation="Horizontal"
                Spacing="12"
                x:Load="{x:Bind ViewModel.ShowNewUserAction, Mode=OneWay}">
                <Button Content="New User" Command="{x:Bind ViewModel.NewUserCommand, Mode=OneWay}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
