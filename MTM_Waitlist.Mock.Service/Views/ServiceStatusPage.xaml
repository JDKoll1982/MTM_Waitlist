<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MTM_Waitlist.Mock.Service.Views.ServiceStatusPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MTM_Waitlist.Mock.Service.Models"
    mc:Ignorable="d">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="12" MaxWidth="1100" HorizontalAlignment="Stretch">

            <TextBlock
                Style="{ThemeResource TitleTextBlockStyle}"
                Text="{x:Bind ViewModel.TitleText}" />

            <InfoBar
                IsOpen="True"
                IsClosable="False"
                Severity="Informational"
                Message="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" />

            <Button
                Command="{x:Bind ViewModel.LoadStatusCommand}"
                Content="{x:Bind ViewModel.ReloadText}" />

            <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="{x:Bind ViewModel.SummaryHeaderText}" />

            <StackPanel Spacing="4">
                <TextBlock Text="{x:Bind ViewModel.ServiceVersionText, Mode=OneWay}" />
                <TextBlock Text="{x:Bind ViewModel.StartedText, Mode=OneWay}" />
                <TextBlock Text="{x:Bind ViewModel.VisualSourceText, Mode=OneWay}" />
                <TextBlock Text="{x:Bind ViewModel.CredentialText, Mode=OneWay}" />
                <TextBlock Text="{x:Bind ViewModel.RefreshScheduleText, Mode=OneWay}" />
                <TextBlock Text="{x:Bind ViewModel.BackupToolText, Mode=OneWay}" />
                <TextBlock Text="{x:Bind ViewModel.AutoStartText, Mode=OneWay}" TextWrapping="Wrap" />
            </StackPanel>

            <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="{x:Bind ViewModel.ShapesHeaderText}" />

            <ListView ItemsSource="{x:Bind ViewModel.Shapes, Mode=OneWay}" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:ShapeStatusRow">
                        <StackPanel Spacing="2" Padding="0,6">
                            <TextBlock FontWeight="SemiBold" Text="{x:Bind ShapeKey}" />
                            <TextBlock Text="{x:Bind OutcomeText}" />
                            <TextBlock Text="{x:Bind LastRunText}" />
                            <TextBlock Text="{x:Bind RowCountText}" />
                            <TextBlock Text="{x:Bind FreshnessText}" TextWrapping="Wrap" />
                            <TextBlock
                                Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                Text="{x:Bind ValidationErrorText}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="{x:Bind ViewModel.BackupsHeaderText}" />

            <ListView ItemsSource="{x:Bind ViewModel.Backups, Mode=OneWay}" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:BackupStatusRow">
                        <StackPanel Spacing="2" Padding="0,6">
                            <TextBlock FontWeight="SemiBold" Text="{x:Bind Store}" />
                            <TextBlock Text="{x:Bind OutcomeText}" />
                            <TextBlock Text="{x:Bind LastRunText}" />
                            <TextBlock Text="{x:Bind ScheduleText}" />
                            <TextBlock Text="{x:Bind ArtifactCountText}" />
                            <TextBlock Text="{x:Bind ArtifactPathText}" TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </ScrollViewer>
</Page>
