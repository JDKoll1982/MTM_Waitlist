# Findings Fixed Log

> Auto-generated by `speckit.fix-findings` (unattended) on 2026-09-20, against the cross-artifact analysis that
> returned **FAIL** for `specs/006-user-management-and-permissions`.

## Summary

- **Total iterations**: 1 fix pass, then 1 re-analysis
- **Findings identified**: 20 — 2 CRITICAL, 7 HIGH, 7 MEDIUM, 4 LOW
- **Findings resolved**: 19 fully; 1 (L3) partially, with the remainder argued down
- **Findings deferred**: 0
- **Final status**: **CLEAN** — no CRITICAL, no HIGH, no MEDIUM outstanding; the re-analysis verdict moved to PASS

## Scope note, and one deliberate deviation from the registered command body

The registered `speckit.fix-findings` body says "Do NOT modify spec artifacts (`spec.md`, `plan.md`, `tasks.md`) —
only fix implementation code". This run is the opposite by the owner's explicit instruction: the artifacts under
repair *are* the specification artifacts, because nothing in this feature is implemented yet and the FAIL is a
cross-artifact consistency FAIL rather than a code FAIL. The repair was therefore applied to the specification, its
plan, its research, its data model, its three contracts, its task list, its quickstart and its requirements
checklist. No application code was touched. Everything else in the body (parse → fix → re-analyze → log) was
followed.

## Iteration 1

### Findings identified

- [CRITICAL] **C1** — the baselines' home: FR-046 said the one declaration carries each role's baseline while
  FR-048 says the baselines are data, not code. FR-046, FR-075, T016 and T060 were never amended to the plan's
  choice.
- [CRITICAL] **C2** — `permission.admin.reset_password` is pinned as one of the fourteen keys but no task gives it
  a gate site, so the declaration would fail its own FR-062 audit.
- [HIGH] **H1** — the gate-site count disagreed across the spec (twelve), the plan (twelve plus the badge) and the
  task list (thirteen across "eight projects").
- [HIGH] **H2** — the plan and the task list described all twelve (or thirteen) lists as "replaced by named
  permissions", contradicting FR-058 and seed decision 15.
- [HIGH] **H3** — T027 extended the shipped role seed to nine roles including `it_department` one phase before
  T032 performed that rename, so two seeds owned the same rows and the reversal's stated reason stopped being true.
- [HIGH] **H4** — FR-009 ("a person MUST hold exactly one role") was contradicted by FR-017 and the data model and
  was cited by no task.
- [HIGH] **H5** — FR-035 (a reset must not end a session) had no task and no test, and the seed's live case
  "deactivate without session revocation" had no owner.
- [HIGH] **H6** — T017 and T026's file sets omitted the code that produces and reads the role display name, so the
  build and the suite would break.
- [HIGH] **H7** — the three keys with no retired list had their baseline sets only in prose, with no task and no
  test, and SC-001 was undefined for them.
- [MEDIUM] **M1** — the plan carried only the first of the two settled orderings.
- [MEDIUM] **M2** — Phase 8 and 9 create four types neither the plan's structure nor the UI contract lists.
- [MEDIUM] **M3** — T073 and T075 both write `quickstart.md` while both carry `[P]`.
- [MEDIUM] **M4** — the pinned literals and the legacy value `0000` appear in no task's wording.
- [MEDIUM] **M5** — FR-030 and SC-007 say the PIN cannot be read back anywhere while T013 stores a salted hash, and
  T073 searched only the application and its logs.
- [MEDIUM] **M6** — §6 gate 2's eleven live cases had no owner, and two of its cases appeared nowhere.
- [MEDIUM] **M7** — FR-114 was cited by no task.
- [LOW] **L1** — `InlineSqlAuditTests` and `RetiredSymbolAuditTests` were required but named by no task.
- [LOW] **L2** — T074 listed the test project and the solution as files it may modify.
- [LOW] **L3** — six requirements carried no threshold.
- [LOW] **L4** — the count and the pinned strings were asserted in three artifacts with no test behind the number.

### Fixes applied

- **C1** — Took the plan's choice (baselines are seeded data) and amended the specification to match it. FR-046 now
  names the declaration's fields as key, label, what it gates, area, shipped fallback and gate sites and says
  explicitly that it carries no per-role baseline. FR-047 now requires the check in **both** directions. FR-048
  cites itself as the rule that settles where the baselines live. FR-075, US4 scenario 6, the Permission entity,
  `data-model.md` § Permission, `contracts/permission-contract.md` § The declaration, T016, T036 and T060 were
  aligned. The resolution is recorded as deliberate in the specification's Clarifications, in
  `research.md` § "How the baselines satisfy both 'one declaration' and 'data, not code'" and in
  `checklists/requirements.md`.
- **C2** — Named `permission.admin.reset_password` as the gate on the reset action in T048 (the view model that
  decides the action's availability) and T051 (the view), extended T024 so it asserts that key is read by a named
  gate site, and named the gate site in the permission contract's table.
- **H1** — Adopted one canonical sentence, placed once in the specification's Context section: twelve retired role
  lists in nine files across five projects, thirteen gate sites under audit in ten files, eleven of the twelve
  becoming permissions. Every other artifact now points at it, and "eight projects" is gone.
- **H2** — The plan's Scale note and Summary and the task list's Phase 5 Goal and Independent test now say eleven
  lists become named permissions and the twelfth, the badge map, is re-keyed to role codes and stays out of the
  permission set.
- **H3** — Restored T027 to adding `material_handler_lead` and the rungs only, leaving `admin` and `it_department`
  entirely to T032. `contracts/database-contract.md` § Seeds, `research.md` § "The role rename, and how far its
  reversal is exact" and the plan's structure line were made to agree, including the reason the restriction is
  better than stating an application order between two seeds.
- **H4** — FR-009 now says a person is created with exactly one role, that nothing in the feature adds a second,
  that the stored assignments still permit more, and that readers take the highest-ranked live role. FR-001 and the
  Person entity no longer duplicate the "exactly one" claim, and T011 now cites FR-009.
- **H5** — FR-035 is now cited in T013, T066 and T072, and T072 owns both session cases the seed names.
- **H6** — T017's file list gained the three Core models that carry the sign-in role plus `StartupCoordinator`,
  `SplashViewModel`, `ControlInspectorService` and `TooltipService`; T026's gained `StartupOptionsModelsTests`,
  `ControlInspectorServiceTests`, `TooltipServiceTests`, `StartupCoordinatorTests` and `SplashViewModelTests`.
- **H7** — The three baseline sets are now stated in T036 (six roles for the accounts and reset keys, three for the
  permissions key), asserted in T024 and T034, restated in the permission contract, and named in SC-001 and
  quickstart step 5.
- **M1** — The plan's Scale note now carries both orderings, plus a paragraph after the Constitution Check naming
  them as gates and pointing at the tasks that encode them.
- **M2** — The plan's structure gained `IPermissionAdministrationService`, `PermissionAdministrationService`,
  `PermissionHoldersViewModel`, `Models/PermissionHoldersView.cs` and `Module_Settings/Views/PermissionHoldersView`;
  `PermissionsViewModel` no longer claims the who-holds-this view; and the UI contract's test surface lists all four.
- **M3** — `[P]` is dropped from T074 and T075, and the wave heading states that T073, T074 and T075 all record into
  the one evidence document and run in that order while T076 reads only.
- **M4** — T016 names the specification's Verbatim Constraints as the one canonical source for the pinned spellings
  and says no other task restates the literals; T035 reads the keys from the declaration; T008 and T066 name the
  legacy value `0000`.
- **M5** — FR-030 now defines "read back" as plaintext, permits only a salted hash to be stored, and forbids the PIN
  in the store, the logs and the audit row. SC-007 was widened to the store, T073 now searches the store as well,
  and `data-model.md` § Temporary credential and `contracts/database-contract.md` were aligned.
- **M6** — T020 asserts uppercase normalisation (`jsmith` → `JSMITH`, signing in either case), T053 asserts
  employee-number non-uniqueness with `6229` shared by two people, T074 names all eleven of §6 gate 2's live cases,
  and T072 owns the two session cases.
- **M7** — FR-114 is cited in T044 and T059.
- **L1** — T074 names both audit suites and cites §6 gate 4.
- **L2** — T074 is re-filed against the evidence document and states that it modifies no project or solution file.
- **L3** — FR-095 gained "a roster of 1,000 people", FR-115 gained "1,000 rows", and FR-116 gained an upper bound of
  200 percent. SC-011, the UI contract's scaling paragraph and T076 were aligned to the same bound. See the
  argued-down list below for the three that were left alone.
- **L4** — The count is stated once, in the specification's Context, and every other mention points at it. The
  fourteen pinned keys now live in exactly two places — the specification's Verbatim Constraints and
  `contracts/permission-contract.md` — and were removed from `data-model.md`.

### Findings argued down

- **L3, in part** — FR-082 ("a short description"), FR-088 ("clearly marked") and FR-097 ("a clearly separated
  area") were left as written. Each names a property that a reader recognises on sight and that the corresponding
  acceptance scenario already fixes: US1 scenario 3 for the switched-off marking, US2 scenario 18 for the
  fields-above/actions-below split, and the Administration entries' descriptions are pinned by the resource-key
  prefixes and asserted by T046. Adding a numeric threshold to a visual property would create a number no one can
  check, which is worse than the word the scenario already defines.

### Findings deferred

None. The two judgement calls the owner flagged — C1's baselines' home and H3's seed ordering — were both resolved
in the direction the owner instructed, and neither needed a human decision to complete.

## Re-analysis

The registered body's Step 5 says to delete `analysis.md` and re-run `speckit.analyze`. **`speckit.analyze` cannot
be dispatched from inside another command in this session** — there is no tool that runs a sibling command — so the
command body's own fallback was used: the same cross-artifact check was performed directly and written to
`analysis.md`. It is labelled there as the agent's own check rather than presented as a dispatched analyzer run.

| Measure | Before | After |
| --- | --- | --- |
| Verdict | FAIL | **PASS** |
| CRITICAL | 2 | 0 |
| HIGH | 7 | 0 |
| MEDIUM | 7 | 0 |
| LOW | 4 | 0 fixed-needed (3 addressed, 3 sub-items argued down inside L3) |
| Requirement coverage | 114 of 117 (97.4%) | **137 of 137 (100%)** — 117 FR and 20 SC, no orphans |
| Task citations not in the spec | — | 0 |
| Tasks / phases / waves | 76 / 12 / 36 | 76 / 12 / 36 (T074 and T075 lost their `[P]` markers; no task was added or removed) |

## Residual risk, stated rather than hidden

- The canonical count rests on the earlier analyzer's own reading of the shipping code plus this pass's
  confirmation that every named file exists in the project it is attributed to. Neither pass exhaustively searched
  the workspace for a further hand-written role list, and neither counted each list's membership. T002 and T035 own
  that proof, and they remain the things that would catch a fourteenth site.
- Nothing in this feature is implemented, so no build and no test was run and none could be. The verification here
  is a document-level consistency check, which is what the gate asked for.
- `.spec-context.json` was corrected only through `write-context.py`. Four writes were refused mid-run by the known
  `[WinError 5]` atomic-rename fault on this machine; each was retried and each retry was read back and confirmed.
  The file parses and no stray temporary file was left behind.
