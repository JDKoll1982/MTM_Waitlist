# Readiness Checklist — Close Out Before the Next Major Implementation

**Generated:** 2026-09-20 · **Branch:** `004-unified-card-item-picker` · **HEAD:** `4c1096b` · **Working tree:** clean (0 modified files)
**Active feature pointer:** `.specify/feature.json` → `specs/004-unified-card-item-picker` · **Spec state:** `.spec-context.json` = `implement` / `implementing`
**Scope:** everything that must be finished, corrected, or decided to bring the **current codebase** to a genuine 100% — the open gates in the in-flight spec, the documentation that no longer matches the code, the database artifacts, the guards that cannot fail, and the verifications only a person can do.
**Out of scope, by the owner's instruction (2026-09-20):** choosing or seeding the **next workstream**. This checklist exists to make the current tree ready, not to plan what comes after it — decisions D-5 and D-6 were withdrawn for that reason and are recorded below as withdrawn rather than deleted.

**Where this work is carried (2026-09-20).** Phases 2–4 below are mirrored as work items in **`specs/004-unified-card-item-picker/tasks.md` Phase 14 (T189–T228)**, so an automated implementer can drive them — those commands read `tasks.md` and nothing else. **This file stays the detail of record**: it holds the argument and the evidence behind each item, and it is where the three owner-only actions in Phase 5 live, deliberately excluded from the task list so nothing can tick them without a person.

---

## Decisions

Every decision this work depends on, in one place. **All five live decisions are answered as of 2026-09-20, and nothing in this file is blocked on a question any more** — each answer is folded into the phase beside it. D-5 and D-6 were withdrawn: they were about the next workstream, which is out of scope.

| # | Decision | Status | The answer, or the question being asked | Where it lives | Blocks |
| --- | --- | --- | --- | --- | --- |
| **D-1** | Where a chosen die is stored per request (spec `T180`) | **ANSWERED 2026-09-20** | *A die always goes to its own home location and the person is never asked where it goes.* The destination question retires, which frees the request's one value column: `input_value` carries **which die the request is for**. **No schema change** — no `die_identifier` column, no procedure pair, no aggregate, no reinstall. | `spec.md` FR-053/FR-054 + the decision note; `research.md` **D22**; `data-model.md` §6/§7; the three contracts; the CSV's two die rows; `tasks.md` T180 (ticked) | Nothing — it unblocks T186, T181–T183 |
| **D-2** | Does `deliver-die` also ask which die, and what does the die step offer? | **ANSWERED 2026-09-20** | *Yes — both die Items ask, with an option to select all, and all die locations are shown on the details page.* So the die step offers **every** die the job carries **plus a select-all action**, and choosing all of them raises one request per die. The **request page lists every die location the job carries**, not only the location of the die that request names. Recorded as `spec.md` FR-054 (select all) and the new **FR-057** (the request page's die locations), the flow contract's two die rows, and `tasks.md` T182/T187/T188. | `spec.md` FR-054 and FR-057; `research.md` D22 "Consequences"; `contracts/request-picker-flow.md`; CSV row 17 `deliver-die`; `tasks.md` T182, T187, T188 | Nothing — folded into Phase 1.1 |
| **D-3** | The three duplicated table folders | **ANSWERED 2026-09-20** | *Validate all three pairs are identical, delete the extras, then validate nothing else points at them.* Three ordered steps: **(a)** diff each pair byte-for-byte and stop if any pair differs — a difference means one of them is live behaviour, not litter; **(b)** delete the superseded folder in each pair; **(c)** validate every stored procedure, view, function and aggregate references only the surviving table, **and** check whether the duplicate creates came with duplicate **procedures** — if they did, apply the same treatment to those, validating before deleting. | Phase 3.1; `Database/Tables/02_*`, `13_*`, `14_*` | Phase 3.2 and 3.3 |
| **D-4** | Do the locked C#/XAML naming rules still bind as written? | **ANSWERED 2026-09-20** | *Retire the no-grandfather rule, but still produce a report of every violation for future normalization.* So the rules file keeps its hard-fail discipline for **new and changed** code and loses the "legacy naming must be bulk normalized, no grandfathering" clause; the **901 findings are recorded as a report** — per rule, per project, with counts — to be normalized later rather than now. | Phase 4.2; `.github/instructions/csharp-xaml-naming-rules.instructions.md` | Nothing — folded into Phase 4.2 |
| ~~D-5~~ | ~~The next workstream~~ | **WITHDRAWN 2026-09-20** | Withdrawn by the owner: the checklist is scoped to making the **current** codebase 100% ready, so choosing what comes next is not its work. Recorded here so the removal is visible rather than silent. | — | — |
| ~~D-6~~ | ~~Analytics' five prerequisite questions~~ | **WITHDRAWN 2026-09-20** | Withdrawn with D-5 for the same reason. The questions still stand in `OPEN-WORK-NEXT-SPEC.md` §6.5 and belong to whoever specifies that feature. | — | — |
| **D-7** | The 11 SQL naming violations the crash was hiding | **ANSWERED 2026-09-20** | *Same as D-4* — do **not** rename them. The eleven stay, the rules lose the expectation that legacy SQL must be clean, and they are **recorded in a report** for a later normalization pass. The crash still has to be fixed, because a guard that dies instead of reporting is the actual defect. | Phase 4.1; the eleven findings are listed in that phase | Nothing — folded into Phase 4.1 |

---

> **How this was built — and what "verified" means here.** Every claim below was read out of the repository *and then checked against the code, the database artifacts, or a run*. Nothing is carried over from a document's own "complete" statement. Four things were executed on this machine, and each finding that came from a run says so:
> - `dotnet build MTM_Waitlist.sln -c Debug -p:Platform=x64 /m:1 /nodeReuse:false`
> - `dotnet test MTM_Waitlist.Tests/MTM_Waitlist.Tests.csproj -c Debug -p:Platform=x64`
> - `.github/scripts/validate-sql-naming.ps1`
> - `.github/scripts/validate-csharp-xaml-naming.ps1`
>
> Where a document said a thing was done and the code agreed, this file says so plainly. Where a document said something the code contradicts, that contradiction is a task.

---

## Verified baseline (2026-09-20, this pass)

| Check | Result | Note |
| --- | --- | --- |
| Solution build | **`0 Warning(s) 0 Error(s)`** | Full solution, 31 s |
| Test suite | **`Failed: 0, Passed: 1082, Skipped: 27, Total: 1109`** | 33 s |
| Suite size vs the recorded evidence | **+20 tests** | `specs/004/tasks.md` last records **1089** (T179) and `WhatINeedToDo.md` repeats 1089 — the tree has moved since that evidence was written |
| Retired-symbol gate | **Healthy** | `MTM_Waitlist.Tests/Module_Mock/RetiredSymbolAuditTests.cs` carries real patterns *and* a self-biting test proving each pattern still matches its own symbol |
| Category/Item model (specs/004 FR-023) | **Shipped** | `RequestItemCatalog`, `RequestItemDefinition`, `RequestItemLine2Resolver` present; the retired type/subtype tables survive **only** as `rollback.sql` drops plus the recorded retirement note — the permitted exemptions |
| Dunnage step (specs/004 FR-048…FR-051) | **Shipped** | `NewRequestDunnageViewModel`, `NewRequestDunnagePage.xaml`, `IDunnageSubstitutePicker`, the `list` key on `RequestItemFieldDefinition`, and the routing branch in `NewRequestFlowRules` all present |
| Die rules (specs/004 FR-055/FR-056) | **Shipped** | `MTM_Waitlist.Core/Services/DieDecisionRules.cs`, `RequestDiePart.ComposeLabel` |
| One request per die (specs/004 FR-054) | **DECIDED 2026-09-20, not built** | T180 is answered — a die goes to its home location, the destination question retires, and `input_value` carries the chosen die with no schema change. Still absent from the tree: the die step, the die routing branch and the per-die submission |
| Transition guard (specs/004 FR-043) | **Shipped** | `sp_waitlist_request_status_update` takes `p_expected_status` and conditions the update on it |
| Sign out (specs/004 FR-033/FR-034) | **Shipped** | `ISignOutService`, `SignOutService`, badge command on `ShellViewModel` |
| Cache fallback (specs/001) | **Shipped** | Five `IVisualReadFallback` registrations, five `sp_visual_*_get` + `_refresh` pairs under `Database/Mock/` |
| Role authorization replaced the shared credential (T147) | **Shipped** | `sp_auth_user_row_get`, `ServiceOperatorAuthenticationHandler`; `SharedToken*` gone |
| Capability-aware service (T167) / host fallback (T165, T171) | **Shipped** | `ServiceCapabilityProbe`, `MySqlHostFallback`, `MySqlConnectionStringResolver`, `ServiceLog`, `ServiceFileLoggerProvider` |
| Eight test accounts seeded (T157) | **Shipped** | `seed_dev_masked_baseline` holds the eight `test.*` accounts, five approved and three deliberately not |
| Cached-data settings panel (T160) | **Shipped** | `MockServiceClientOptions` + the "Cached Infor Visual data" card; `appsettings.json` values empty on purpose |
| Publish profile (T159) | **Shipped** | `Properties/PublishProfiles/win-x64-selfcontained.pubxml` |
| **SQL naming validator** | **BROKEN** | Crashes before it can report; **11 real violations** are hidden behind the crash — Phase 4 |
| **C#/XAML naming validator** | **901 findings** repo-wide | CI checks only changed files, so the backlog has never surfaced — Phase 4 |

**Two things this pass could not verify, and does not claim:** the app has not been driven on screen (no UI walk was run), and no live `mtm_waitlist` install was executed — those are the owner-gated steps in Phase 5.

---

## Phase 1: Finish the in-flight implementation (`specs/004-unified-card-item-picker`)

`specs/004/tasks.md` now holds **58 open boxes** — **18** belonging to this feature (the die and card work above) and **40** belonging to Phase 14, which is this file's Phases 2–4 re-homed as tasks. Nothing in this phase is documentation; all of it is the feature that is currently half-built. Its one blocking decision, **D-1**, is answered — see the [Decisions](#decisions) table at the top.

### Subphase 1.1: The die decision — **answered** (Decisions D-1 and D-2)

- [x] **Decision: record where a chosen die is stored per request — T180.** **ANSWERED 2026-09-20 by the owner:** *a die always goes to its home location and the user is never asked where it goes.* The destination question therefore retires, which frees the request's one value column: `input_value` carries **which die the request is for**. **No schema change** — no `die_identifier` column, no procedure pair, no aggregate, no reinstall. Recorded in `spec.md` (FR-053, FR-054 and the decision note), `research.md` D22, `data-model.md` §6/§7, `card-and-identifier.md`, `request-picker-flow.md`, `verification-gates.md`, the CSV's two die rows and `tasks.md` Phase 13. (Ref: `specs/004-unified-card-item-picker/research.md` D22) | **Persona: Tech Lead** — verified 2026-09-20: the decision and its propagation are written into the eight files named above; `tasks.md` T180 is ticked
- [ ] **Schema Artifact: retire the destination question from the configuration seed — T181.** (Decisions **D-1**, **D-2**) `pickup-die` loses its required answer, prompt, value type and options; **both** die rows declare the `die` job list, which moves `deliver-die` off `direct-to-confirmation` so it too reaches the die step. `AllSeeds.sql` moves in the same change. No column, no procedure pair and no aggregate is touched. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 13, T181) *Depends on: T180 decision* | **Persona: Database Engineer**
- [ ] **Test Debt: pin the retirement and the new die behaviour with tests that fail first — T186.** A raised die request resolves *its own* die, so two entries from one job stay distinguishable; **select-all raises one request per die the job carries**; the request page lists **every** die location; and no source, catalog row or configuration row names the retired `destination` token. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 13, T186) *Depends on: T180 decision* | **Persona: QA Engineer**
- [ ] **Data Model + Service Layer: carry the chosen die on `WaitlistRequestDraft` and `WaitlistRequest` and through the service mapping — T181.** A raised die request must resolve *its own* die rather than the job's first one, which is what makes several entries from one job indistinguishable today. No new property is stored beyond the value the request already carries; the change is that a die Item now writes the die into it. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 13, T181) *Depends on: T180 decision* | **Persona: Backend Engineer**
- [ ] **Workflow: build the die step — `NewRequestDieViewModel` + `NewRequestDiePage`, reached because the chosen Item's configuration names the `die` job list, offering the job's dies as selectable cards that may be selected more than once, **with an option to select all** — T182.** (Decisions **D-1**, **D-2**) Put the check **before** the `!requiresAnswer || answerCaptured → Preview` branch, or `deliver-die` (`direct-to-confirmation`) never reaches the step. Each card shows the die's number and its home location, so the operator chooses by seeing where each die lives. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 13, T182; `spec.md` FR-054) *Depends on: T180 decision* | **Persona: Full Stack Engineer**
- [ ] **Workflow: show every die location on the request page — T187.** (Decision **D-2**, and the new `spec.md` **FR-057**) A die request's page currently resolves the job's **first** die; it must instead show **every** die location the requesting job carries, so a handler reading one request can see where the job's dies live. Read the dies once per work centre, render them all, and invent nothing for a location the job does not supply. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 13, T187; `spec.md` FR-057) *Depends on: T180 decision* | **Persona: Full Stack Engineer**
- [ ] **Verification: prove the request page names every die location — T188.** Walk T187 in the running app: open a die request raised from a job carrying **two** dies and prove from a text dump that the page names **both** locations, not only the request's own die. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 13, T188) *Depends on: T187* | **Persona: QA Engineer**
- [ ] **Workflow: raise one request per selected die — T183.** Never one request carrying several dies; report how many were raised. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 13, T183) *Depends on: T182* | **Persona: Full Stack Engineer**

Next task: **pin the retirement and the new die behaviour with tests that fail first (T186)** | **Persona: QA Engineer**

### Subphase 1.2: The Phase 9 walk that was never finished (6 boxes)

- [ ] **Verification: raise the nineteen-request population through the application — T129.** Nineteen requests, one per in-scope Item, raised as the ten existing accounts with every one of the eight roles represented and spread across more work centres than the seven carrying the prepared situations; every material-dependent Item raised from a job that actually carries that material; all nineteen left unhelped. Evidence: the nineteen rows read back with their `category`, `item`, `status` and requester. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 9 Wave 7; FR-036…FR-040) | **Persona: QA Engineer**
- [ ] **Verification: exercise handling outcomes 1 and 2 — T130.** Accept-then-finish, and accept-then-hand-back-then-accept-again-then-finish. A hand-back must leave **one** request, back to `Pending`, with `released_utc` set and the handler cleared. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 9 Wave 8; FR-042) *Depends on: 1.2 population* | **Persona: QA Engineer**
- [ ] **Verification: exercise handling outcome 3 — T131.** The raiser cancels before anyone accepts, signed in as that request's own raiser, and a non-raiser cannot. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 9 Wave 8) | **Persona: QA Engineer**
- [ ] **Verification: exercise handling outcomes 5 and 6 — T133.** One handler hands back and a **different** handler takes it over; and a handler accepts a request they raised themselves, which must be **permitted**. Prove the first handler's assignment is gone and the second handler's stands. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 9 Wave 8; FR-041) | **Persona: QA Engineer**
- [ ] **Database Validation: apply and reverse every paired `create.sql` / `rollback.sql` the batch touches against a local `mtm_waitlist` — T137.** Confirm `AllSeeds.sql` and `Bootstrap/update_table_descriptions.sql` agree with the files on disk and that the seven prepared situations resolve from real work centres. **The reinstall is the owner's action, never the agent's.** (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 9 Wave 9) | **Persona: Database Engineer**
- [ ] **Verification: walk SC-013 … SC-020 against the recorded evidence — T139.** SC-013 on the sign-out gate, the population on T129, the outcomes on T130–T134, the build/test criteria on T135–T136 — **do not run the suites a second time**. Any criterion without recorded evidence stays unticked and is reported. (Ref: `specs/004-unified-card-item-picker/spec.md` SC-013…SC-020) *Depends on: 1.2 population, 1.2 outcomes* | **Persona: QA Engineer**

Next task: **raise the nineteen-request population (T129)** | **Persona: QA Engineer**

### Subphase 1.3: The UI and live-database gates still open (4 boxes)

- [ ] **Verification: prove the dunnage step in the running app — T164.** From a text/geometry dump, not from the code: the step sits between Item and Preview; the job's parts are drawn as cards; Continue is reachable only after a part is chosen; `Use substitute…` opens the receiving catalogue and a part chosen there becomes the request's value; the raised request's card shows the chosen part on its second line. Needs a signed-in session and a job carrying dunnage. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 11 Wave 6) | **Persona: QA Engineer**
- [ ] **Database Validation: re-run the configuration seed and read both dunnage rows back with their `list` key intact — T165.** Confirm `AllSeeds.sql` agrees with the file on disk. **The reinstall is the owner's action, never the agent's.** (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 11 Wave 6) | **Persona: Database Engineer**
- [ ] **Verification: prove the die card reads the job — T174.** Raise a pickup-die and a deliver-die request from a work centre whose job carries a die (the seed's `100-7`) and prove from a text dump that Line 1 reads `Pickup Die: <the job's part number>` / `Deliver Die: <…>` and Line 2 reads `<FGT number>-<die location>`. Then move the die's location in the store and reload, to prove the card follows the job rather than a stale copy. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 12 Wave 4) | **Persona: QA Engineer**
- [ ] **Database Validation: confirm the die values resolve from the live read — T175.** `sp_setup_active_jobs_latest_by_work_center_get` must return the job's `part_number` and the die row's `PartNumber` / `Location` for a real active job, so the two lines have something to resolve against outside the seed. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 12 Wave 4) | **Persona: Database Engineer**

Next task: **prove the dunnage step in the running app (T164)** | **Persona: QA Engineer**

### Subphase 1.4: Close the spec honestly

- [ ] **Spec State: tick each box only as it is verified, then record the gate evidence in `tasks.md` with today's numbers — not the stored ones.** The recorded test figure is stale: the suite is now **1109 total / 1082 passed / 0 failed / 27 skipped** where `tasks.md` last recorded 1089. (Ref: `specs/004-unified-card-item-picker/tasks.md` Phase 9 W9, Phase 11 W6, Phase 12 W4, Phase 13 W3) *Depends on: all of Phase 1* | **Persona: QA Engineer**
- [ ] **Spec State: reconcile `specs/004/spec.md`'s header with the recorded status.** The header still reads **`Status: Draft`** while `.spec-context.json` says `implement` / `implementing` and the document's own body calls the feature **delivered** and **shipped**. Three statements, two of them wrong. (Ref: `specs/004-unified-card-item-picker/spec.md` header; `specs/004-unified-card-item-picker/.spec-context.json`) | **Persona: Tech Lead**
- [ ] **Spec State: confirm FR-054, FR-057 and the amended FR-053 read as delivered once the build lands.** The storage question is answered (reuse, not a new column), so FR-054 is **in this feature** rather than descoped — but only once T181, T182, T183 and T187 are built and the three gates are walked. Until then the requirement is open and `spec.md` must say so. (Ref: `specs/004-unified-card-item-picker/spec.md` FR-053/FR-054/FR-057) *Depends on: all of Phase 1* | **Persona: Tech Lead**

**GATE: every remaining `specs/004` box is ticked with named evidence, the build is `0 Warning(s) 0 Error(s)`, and the suite is `Failed: 0` — or the unticked boxes are explicitly reported as open with the reason. A "complete" claim must never rest on an unrun gate.**

---

## Phase 2: Correct the documentation that now contradicts the code

*Carried as tasks in `specs/004-unified-card-item-picker/tasks.md` Phase 14, T189–T212.*

> **Walked 2026-09-20 (T228).** Phases 2–4 were walked box by box and **every box now names the artifact behind it** — each one carries its verifying task inline, and the evidence itself is in `tasks.md` beside that task. The walk was run as 41 named checks against the tree, not read off the documents' own claims; six of those checks failed on first run and five of the six turned out to be bad checks rather than bad work (the `FEATURES.md` pin check demanded the old hash be *absent* when the header correctly records it as the *previous* pin; the defect-table check matched a row that now records both closures; the Mockups check counted 10 referencing files where the box says 12; the schema-rules check matched the corrected sentence *"there is **no** FluentMigrator runner"*; the folder-table check's regex read `IF` as the table name). **One was a real gap**, and it is closed: `.github/instructions/winui3-ui-automation.instructions.md` still asserted that reaching the shell needs a sign-in, which this pass disproved by driving the running app — so the always-on recipe was corrected, together with its verification table. **Phase 5 was left untouched and unticked**, as the task requires: its boxes are the owner's actions, not an agent's.

Every item below was confirmed against the tree, not inferred from a diff. The pattern is the same throughout: a document describes a state that shipped past, or points at a file that no longer exists.

### Subphase 2.1: Root trackers

- [x] **Documentation: correct `OPEN-TASKS.md` §2's carry-forward total and its pointer.** It still reads **127 boxes** with the check line `11 + 20 + 20 + 8 + 39 + 2 + 27 = 127`, and its §5 item 8 still says to specify against **§4 first** — but §4 shipped as `specs/003` and §5 shipped as `specs/004`. The live figure is **116**, the §4 row is delivered, and the pointer belongs on **§6**. (Ref: `OPEN-TASKS.md` §2, §5; `WeekendProject/OPEN-WORK-NEXT-SPEC.md` §12) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T189).
- [x] **Documentation: name `specs/003` and `specs/004` in `OPEN-TASKS.md`.** Neither is mentioned anywhere in the file — it walks from `specs/001` to "new spec" as though two delivered features did not happen. (Ref: `OPEN-TASKS.md` §1, §5) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T190).
- [x] **Documentation: repair the dangling host-verification reference in `OPEN-TASKS.md` §5.** Item 6 cites `VALIDATION-PROMPT-SERVER.md`, which does not exist in the tree; the same stale citations sit inside `specs/001/tasks.md` (Phases 25–39). Classify each as provenance (a record of what was run) rather than instruction before rewriting. (Ref: `OPEN-TASKS.md` §5; `specs/001-module-mock-visual-fallback/tasks.md`) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T191).
- [x] **Documentation: restate the dated follow-up honestly.** The SC-007/SC-008 observation windows opened 2026-09-12 and the re-check is **due 2026-10-12** — a measurement, not a task. Keep it visible in one place and mark it as unmeasured until then. (Ref: `specs/001-module-mock-visual-fallback/tasks.md` §1.1; `OPEN-TASKS.md` §5) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T192).

Next task: **correct the carry-forward total and the next-spec pointer in `OPEN-TASKS.md`** | **Persona: Tech Lead**

### Subphase 2.2: `FEATURES.md` — pinned to a commit two features behind

- [x] **Documentation: re-verify `FEATURES.md` against the current tree and move its pin.** It declares **"Last verified against: the working tree at commit `7bf6857` (2026-09-12)"** — before `specs/003` and `specs/004`. (Ref: `FEATURES.md` header) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T193).
- [x] **Documentation: delete the now-false "not yet built" claims.** It states *"The handler list is not sorted most-urgent-first. The due and overdue sums are correct; the ordering is not applied."* — the ordering **is** applied: `UrgencyCalculator.OrderBy` defaults to most-urgent and `WaitlistViewViewModel.SortOrder` resolves the remembered key, defaulting to `WaitlistSortOrder.MostUrgent`. The same section's claim that the material attributes are unsourced and that analytics/user management "has not started" needs re-checking line by line, not by assumption. (Ref: `FEATURES.md` "Not yet built"; `MTM_Waitlist.Core/Services/UrgencyCalculator.cs`; `MTM_Waitlist.Waitlist.View/ViewModels/WaitlistViewViewModel.cs`) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T194).
- [x] **Documentation: extend the maintainer index and drop the dead pointer.** The index lists `specs/001` alone although the defect table cites `specs/002` ten times and `specs/003`/`specs/004` now exist; it also points at **`RELEASE-NOTES.md`**, which is not in the tree. (Ref: `FEATURES.md` "For the maintainer") | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T195).
- [x] **Documentation: reconcile the defect table's status column with the defect files.** `FEATURES.md` says the Cancel/Accept defect is *"Closed by `specs/002` — the buttons are gone"* while the defect file itself says it was **closed by `specs/003`** with the actions working. Two documents, two closures, one truth. (Ref: `FEATURES.md` defect table; `defects/Closed-High-Waitlist-CardCancelAndAcceptButtonsAreInert.md`) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T196).

Next task: **re-verify `FEATURES.md` and move its commit pin** | **Persona: Tech Lead**

### Subphase 2.3: `WeekendProject` sources that still overstate or understate the work

- [x] **Documentation: reconcile `PromptFiles/07-8%-Phase2-fulfill.md` against `specs/003`.** It still shows **11 open boxes** and carries no `specs/003` note anywhere, although that spec's own `## Input` line cites this file as its seed and delivered the workstream. Reconcile every box, delete the two mock-mode boxes §3 of the seed says cannot be built, and leave a "delivered by `specs/003`" note. (Ref: `WeekendProject/PromptFiles/07-8%-Phase2-fulfill.md`; `WeekendProject/OPEN-WORK-NEXT-SPEC.md` §2) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T197).
- [x] **Documentation: reconcile `PromptFiles/08-70%-Phase2-urgency.md` against `specs/003`.** **3 open boxes**, no `specs/003` note, and one box is the mock-mode default that cannot be built. (Ref: `WeekendProject/PromptFiles/08-70%-Phase2-urgency.md`; `WeekendProject/OPEN-WORK-NEXT-SPEC.md` §2) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T198).
- [x] **Documentation: recount `PromptFiles/13-63%-Developer-UI.md` with `+ [ ]` included.** It holds **4 `+ [ ]` boxes**, not the 0 a `- [ ]`-only sweep reports; one is the startup-gate polish that is still genuinely open, one is a race-guard review, and the remaining two reference a file that does not exist. (Ref: `WeekendProject/PromptFiles/13-63%-Developer-UI.md`) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T199).
- [x] **Documentation: retire the dangling artefact references rather than leaving them.** 12 `PromptFiles/*` point at `../Mockups/*.svg` and the folder is not in the tree; `PromptFiles/13` also names `WeekendProject/DeveloperUI-VisualPrompts.md`, which is not in the tree. Classify each as instruction vs provenance before editing — the three want different answers. (Ref: `WeekendProject/PromptFiles/02`, `03`, `04`, `05`, `06`, `07`, `08`, `09`, `10`, `11`, `13`) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T200).
- [x] **Documentation: correct `WeekendProject/Module_Mock/Module_Mock-Planning-Progress.md`'s status line.** It opens with **"Status: In design/planning. Not implemented."** — the capability shipped in `specs/001` and its own `Tasks.md` was reconciled against the shipped code. (Ref: `WeekendProject/Module_Mock/Module_Mock-Planning-Progress.md`; `WeekendProject/Module_Mock/Tasks.md`) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T201).
- [x] **Documentation: add `07` and `08` to the retired-sources note.** `OPEN-TASKS.md` §3.1 names six retired sources and deliberately omits these two; because they are now delivered-but-unreconciled they are the same class of counting trap, and the note should say so. (Ref: `OPEN-TASKS.md` §3.1; `WeekendProject/OPEN-WORK-NEXT-SPEC.md` §2) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T202).
- [x] **Documentation: reconcile `App-Validation-Checklist.md` with the delivered card work.** It holds **45 unchecked** boxes, of which §4–§8 (27) were the carry-forward for the card work that `specs/004` has since delivered — and at least one check names the **retired** catalog (`waitlist_request_types` / `_subtypes` exposing `category` + `item_id`), which no longer exists. Left as it is, the next person to reuse this as an acceptance suite would fail a shipped feature. This is a document that describes the **current** app, so it belongs to this phase rather than to any future workstream. (Ref: `WeekendProject/PromptFiles/App-Validation-Checklist.md` §4–§8) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T203).

Next task: **reconcile `PromptFiles/07` against `specs/003`** | **Persona: Tech Lead**

### Subphase 2.4: Spec state files and directories

- [x] **Spec State: reconcile `specs/003-waitlist-handler-fulfilment/spec.md`'s header.** It still reads `Status: Draft` while `.spec-context.json` records `status: completed` with 49/49 tasks. (Ref: `specs/003-waitlist-handler-fulfilment/spec.md`; `WeekendProject/OPEN-WORK-NEXT-SPEC.md` §1) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T204).
- [x] **Spec State: resolve the orphan `specs/005-setup-work-order-search/` directory.** It contains **only `.trace.jsonl`** — no `spec.md`, no `.spec-context.json`, no plan. Either it is an abandoned `/speckit.specify` attempt that should be removed, or a real feature that was never written; the trace records a lifecycle and several captures before the run stopped. An empty numbered feature directory is a trap for whoever runs the next `specify`. (Ref: `specs/005-setup-work-order-search/.trace.jsonl`) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T205).

Next task: **resolve the orphan `specs/005` directory** | **Persona: Tech Lead**

### Subphase 2.5: The drift ledger and the living specs

- [x] **Documentation: restore or re-derive `capabilities/DRIFT.md`.** Repo memory names it as the tree's **ledger of record** for drift ("what the detector watches, what it found, and the areas it cannot see"), and it is not in the tree — `capabilities/` holds only `startup/` and `startup-diagnostics/`. (Ref: `.github/memories/repo/document-inventory.md`; `living-specs.yml`) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T206).
- [x] **Documentation: state the living-spec blind spot where a reader will hit it.** Only `startup` and `startup-diagnostics` are registered; the whole `MTM_Waitlist.Waitlist.View/**`, `Module_Waitlist/**`, `MTM_Waitlist.Core/**`, `Database/**` and `Strings/**` surface has **no** living spec, so drift there is invisible by construction. A green drift run means the registry is complete, not that the repository is. (Ref: `living-specs.yml`; `capabilities/`) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T207).

Next task: **restore or re-derive `capabilities/DRIFT.md`** | **Persona: Tech Lead**

### Subphase 2.6: Defect files and repo memory

- [x] **Documentation: re-check every defect file's "Planned fix" against the shipped specs.** Thirteen files carry a now-stale ownership line because the features were renamed as they shipped: `Spec 01-truthful-data-and-controls` → `specs/002`, `Spec 03-unified-card-and-taxonomy` → `specs/004`, `Spec 04-waitlist-analytics` → still unbuilt. The **Status** rows are current; the forward-looking rows are not. (Ref: `defects/*.md`, "Planned fix" rows; `WeekendProject/SpecTemplates/00-INDEX.md`) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T208).
- [x] **Documentation: correct the two repo-memory notes that contradict the tree.** `/memories/repo/github-artifact-staleness.md` records `.github/copilot-startup-steps.yaml` and `.github/scripts/restructure-database-layout.ps1` as **DELETED**; both are present — the YAML annotated reference-only, the script still reading the retired FluentMigrator layout. A memory that says a file is gone is worse than no note, because the next agent stops looking. (Ref: `.github/copilot-startup-steps.yaml`; `.github/scripts/restructure-database-layout.ps1`) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (T209).
- [x] **Documentation: fix the stale platform version.** `Directory.Build.props:30` says *"satisfied by the 2.3.0 in use"*; the repository pins **2.3.1**. (Ref: `Directory.Build.props`) | **Persona: DevOps Engineer** -- **VERIFIED 2026-09-20** (T210).
- [x] **Documentation: correct the two stale schema-rule statements in `.github/instructions/database-schema-rules.instructions.md`.** Its "Required Startup-Core Tables (v1)" list still names **`core_workstations_registry`**, a table that no longer exists — the live table is `core_computers_registry`; and its Migration section still describes a **"FluentMigrator runner"** when the repository's model is hand-maintained file-per-artifact SQL with paired `create.sql` / `rollback.sql`. This file is always-on guidance, so both statements actively mislead. (Ref: `.github/instructions/database-schema-rules.instructions.md`; `Database/Tables/02_core_computers_registry/create.sql`) | **Persona: Database Engineer** -- **VERIFIED 2026-09-20** (T211).
- [x] **Documentation: correct the UI-automation guidance now that its stated blocker is gone.** `.github/instructions/winui3-ui-automation.instructions.md` says reaching the shell "needs a sign-in as well, so a full navigation test needs credentials for a valid account", and its verification table records navigation and the shell header as **not verified**. Both are now known to be wrong: verified 2026-09-20, the app reaches the shell with **no sign-in** (auto-sign-in as the configured developer account `johnk`) with both connection-string environment variables set. Update the blocker, the recipe and the verification table together — this file is always-on guidance, so a stale recipe is a stale instruction. (Ref: `.github/instructions/winui3-ui-automation.instructions.md`) | **Persona: Tech Lead** -- **VERIFIED 2026-09-20** (no task number -- closed by the T228 walk).

**GATE: no document in the tree asserts a feature state the code contradicts, and no document points at a file that does not exist without saying it is a historical reference. A reader arriving cold must not be able to start work that is already done.**

---

## Phase 3: Database artifacts — duplicates found in the tree

*Carried as tasks in `specs/004-unified-card-item-picker/tasks.md` Phase 14, T213–T221.*

These were found by reading the artifact folders, not from any document. The schema validators do not catch them today (Phase 3.3 explains why).

### Subphase 3.1: Three folder pairs create the same live table

- [x] **Schema Artifact: prove each of the three pairs is identical before removing anything.** (Decision **D-3**, step a) The Workstation→Computer rename left each table with **two folders whose content creates the same live table**: (`02_core_workstations_registry` / `02_core_computers_registry` → `core_computers_registry`), (`13_setup_workstations_catalog` / `13_setup_work_centers_catalog` → `setup_work_centers_catalog`), (`14_config_workstation_hot_workcenters` / `14_config_computer_hot_work_centers` → `config_computer_hot_work_centers`). Compare each pair's `create.sql` **and** `rollback.sql` byte-for-byte and record the result. **A pair that differs is not litter** — it means one of the two is live behaviour, so stop and report instead of deleting. (Ref: `Database/Tables/02_*`, `13_*`, `14_*`) | **Persona: Database Engineer** -- **VERIFIED 2026-09-20** (T213).
- [x] **Schema Artifact: delete the superseded folder in each pair.** (Decision **D-3**, step b) The folder to remove is the one whose **name** no longer matches the table it creates — the stale half of each pair — leaving the three correctly named folders. Do this only after the identical-content proof above. (Ref: `Database/Tables/02_*`, `13_*`, `14_*`) *Depends on: the identical-content proof* | **Persona: Database Engineer** -- **VERIFIED 2026-09-20** (T214).
- [x] **Database Validation: confirm nothing else points at the removed folders.** (Decision **D-3**, step c) Sweep every stored procedure, view, function, seed and aggregate for a reference to the removed **paths** or names, and confirm the surviving folder is the only definition of each of the three tables. A dangling reference here is a broken install, not a tidy-up. (Ref: `Database/StoredProcedures/**`, `Database/Views/**`, `Database/Functions/**`, `Database/Tables/AllTables.sql`) *Depends on: the deletion* | **Persona: Database Engineer** -- **VERIFIED 2026-09-20** (T215).
- [x] **Schema Artifact: keep the aggregate honest.** `AllTables.sql` contains each of the three tables **once**, so an install from the aggregate and an install from the folders do not apply the same file set. After the deletion the aggregate, the folder set and `update_table_descriptions.sql` must agree, and a fresh install must still produce every table. (Ref: `Database/Tables/AllTables.sql`) *Depends on: the deletion* | **Persona: Database Engineer** -- **VERIFIED 2026-09-20** (T216).
- [x] **Schema Artifact: check whether the same duplication happened to procedures, and treat those the same way.** (Decision **D-3**, step c) The duplicate creates may have come with duplicate **procedures** — two folders defining one procedure. List every procedure defined more than once across `Database/**`, compare each duplicate pair byte-for-byte, delete the stale-named copy, and leave the surviving definition as the only one. Validate before deleting, exactly as above. (Ref: `Database/StoredProcedures/**`, `Database/Mock/StoredProcedures/**`, `Database/StoredProcedures/AllSPs.sql`) | **Persona: Database Engineer** -- **VERIFIED 2026-09-20** (T217).
- [x] **Database Validation: confirm the fresh-install path still works after the cleanup.** Apply the folder set and the aggregate against a local `mtm_waitlist` and compare the resulting table list with the folders on disk. **The reinstall is the owner's action, never the agent's.** (Ref: `Database/install_local_database.bat`; `.github/scripts/validate-database-schema.ps1`) *Depends on: 3.1 decision* | **Persona: Database Engineer** -- **VERIFIED 2026-09-20** (T216).

Next task: **prove each of the three pairs is identical before removing anything** | **Persona: Database Engineer**

### Subphase 3.2: Confirm the retained drop artifacts are intentional (no work expected — verify and record)

- [x] **Schema Artifact: verify the rollback-only folders are by design and say so in one place.** `Database/Tables/20`–`29` (`20_mock_master_tables_registry` … `29_waitlist_request_subtypes`) each hold **only `rollback.sql`** — the retained drops for retired objects, which the retired-symbol audit explicitly exempts. This is correct, and it looks exactly like corruption to a reader who does not know the rule; record the rule wherever the folder layout is documented. (Ref: `Database/Tables/20`–`29`; `MTM_Waitlist.Tests/Module_Mock/RetiredSymbolAuditTests.cs`) | **Persona: Database Engineer** -- **VERIFIED 2026-09-20** (T218).
- [x] **Schema Artifact: confirm the mirror schema lives only under `Database/Mock/`.** The five `visual_*_result` tables plus their `_stage` twins are under `Database/Mock/Tables`, and the `2x_mock_*` folders under `Database/Tables` hold no creates — so there is no second copy of the mirror schema. Verify and record, because the folder names invite the opposite conclusion. (Ref: `Database/Mock/Tables/`; `Database/Tables/AllTables.sql`) | **Persona: Database Engineer** -- **VERIFIED 2026-09-20** (T219).

Next task: **verify the rollback-only folders are by design and record the rule** | **Persona: Database Engineer**

### Subphase 3.3: The schema validator cannot see a duplicate

- [x] **Tooling: teach the schema validator to detect two artifacts creating the same object.** It collects every `Database/Tables/**/create.sql` and applies them in order, so a duplicate create passes silently — the duplicates in 3.1 have survived every CI run because nothing compares folder to object name or counts creates per object. Add the check, or record why the layout makes it impossible. (Ref: `.github/scripts/validate-database-schema.ps1`; `.github/workflows/database-schema-validation.yml`) | **Persona: DevOps Engineer** -- **VERIFIED 2026-09-20** (T220).
- [x] **DevOps: confirm the workflow's failure semantics are real.** `database-schema-validation.yml` runs a script whose table-install phase needs a database connection; confirm whether it can fail for the right reason on a runner with no database, or whether it is passing vacuously. (Ref: `.github/workflows/database-schema-validation.yml`) | **Persona: DevOps Engineer** -- **VERIFIED 2026-09-20** (T221).

**GATE: the table folders, the aggregates and a fresh install agree, nothing dangling points at a removed folder or a deleted duplicate procedure, and the schema validator can fail on a duplicated object. Do not build further schema work on an artifact set that cannot prove itself consistent.**

---

## Phase 4: The guards that cannot fail

*Carried as tasks in `specs/004-unified-card-item-picker/tasks.md` Phase 14, T222–T226.*

Both naming guards are broken, and one of them is broken in exactly the way that hides it.

### Subphase 4.1: The SQL naming validator crashes before it can report

- [x] **Tooling: fix the reserved-variable collision at `validate-sql-naming.ps1:260`.** The failure path is `foreach ($error in $errors)`; `$Error` is a PowerShell automatic **read-only** variable, so the moment there is one violation the script dies with *"Cannot overwrite variable Error because it is read-only or constant"* and **exits 1 without printing a single violation**. The guard fails loudly with the wrong message and no findings. (Ref: `.github/scripts/validate-sql-naming.ps1`; `.github/workflows/sql-naming-compliance.yml`) | **Persona: DevOps Engineer** -- **VERIFIED 2026-09-20** (T222).
- [x] **Documentation: retire the expectation that legacy SQL must already be clean, and record the eleven as a report.** (Decision **D-7** — same answer as D-4) Do **not** rename them. The eleven are: seven filename-pattern failures (`Database/InforVisual/Queues/Module_Mock/Populations/*.sql` — five files — plus `Database/Mock.Service/Restore/replace_database.sql` and `verify_restore.sql`) and four boolean-column names (`require_password_change` in `01_core_users_profiles` and `AllTables.sql`, `requires_answer` in `31_waitlist_request_item_configs` and `AllTables.sql`). Write them into one report file with rule, file and line, state in the rules file that pre-existing violations are grandfathered, and leave the guard failing on anything **new**. (Ref: `.github/instructions/database-schema-rules.instructions.md`; `Database/Tables/01_core_users_profiles/create.sql`; `Database/Tables/31_waitlist_request_item_configs/create.sql`) *Depends on: 4.1 crash fix* | **Persona: DevOps Engineer** -- **VERIFIED 2026-09-20** (T223).
- [x] **Tooling: make the naming script's working-directory assumption explicit.** It derives its root from `$PSScriptRoot` and then `Set-Location`s, so a copy run from anywhere else silently scans the wrong tree and reports "No SQL files found under Database/" — a pass-shaped failure. Fail instead. (Ref: `.github/scripts/validate-sql-naming.ps1`) | **Persona: DevOps Engineer** -- **VERIFIED 2026-09-20** (T224).

Next task: **fix the reserved-variable collision in `validate-sql-naming.ps1`** | **Persona: DevOps Engineer**

### Subphase 4.2: 901 C#/XAML naming findings — grandfathered, and reported

- [x] **Tech Lead: decide whether the locked naming rules still bind as written, or are amended — ANSWERED 2026-09-20: retire the no-grandfather rule, keep the report.** (Decision **D-4**) The rules file keeps its *"naming violations must hard-fail PR checks"* discipline for **new and changed** code — which is what the diff-scoped workflow already enforces — and loses *"legacy naming must be bulk normalized (no grandfathering mode)"*: the 901 existing findings are **grandfathered, not normalised now**. They are recorded in a report for a later pass. (Ref: `.github/instructions/csharp-xaml-naming-rules.instructions.md`; `.github/scripts/validate-csharp-xaml-naming.ps1`) | **Persona: Tech Lead** — verified 2026-09-20: recorded in the Decisions table as D-4; the rules-file and report work is the two tasks below
- [x] **Documentation: amend the rules file to say what is now true.** (Decision **D-4**) Replace the *"Legacy naming must be bulk normalized (no grandfathering mode)"* clause with the rule as answered: pre-existing violations are **grandfathered and tracked in a report**, new and changed code still hard-fails, and an exception still needs written approval. Also state the check's real scope — it inspects the files a pull request changed — so the claim and the mechanism finally agree. (Ref: `.github/instructions/csharp-xaml-naming-rules.instructions.md`; `.github/workflows/csharp-xaml-naming-compliance.yml`) | **Persona: DevOps Engineer** -- **VERIFIED 2026-09-20** (T225).
- [x] **Tooling: generate the violations report for future normalization.** (Decision **D-4**) One report file listing all **901** findings with rule, file and line, grouped so a later pass can take them a rule at a time. The distribution is already measured: `MTM_Waitlist.Tests` **490** (dominated by the `async Task` method must end with `Async` rule), `Strings` **353** (`resw` keys must match `Feature_Element.Property`, including `Service_Shape.work_order_lookup` and `Service_Store.mtm_waitlist`), `MTM_Waitlist.Mock.Service` **18**, `MTM_Waitlist.Core` **15**, `MTM_Waitlist.Settings` **10**, `MTM_Waitlist.Waitlist.View` **8**, `MTM_Waitlist.Setup` **5**, `MTM_Waitlist.Mock` **1**, `MTM_Waitlist.Waitlist.NewRequest` **1**. The report is the deliverable — not the renames. (Ref: `.github/scripts/validate-csharp-xaml-naming.ps1`) *Depends on: the rules-file amendment* | **Persona: DevOps Engineer** -- **VERIFIED 2026-09-20** (T226).

Next task: **amend the rules file to say what is now true** | **Persona: DevOps Engineer**

**GATE: every guard fails for the right reason and prints what it found, and every grandfathered violation is written down in a report rather than forgotten. A guard that crashes, or that only looks at changed files while claiming to police the tree, is worse than no guard — it certifies the wrong thing.**

---

## Phase 5: Verifications no machine on this workstation can do

*Owner permission was granted on 2026-09-20 for the first two of these, and running the checks settled what that permission actually unlocks — item 1 is doable now, item 2 is half-doable, and item 3's real result stays impossible. Nothing here is a question any more; what remains is the part permission cannot reach. **Item 3 moved as far as it can**: its *metric* is now proven against a seeded 30-day history, so what is left is not a question of whether the criterion can be evaluated but of whether the service ever records the history it needs.*

- [x] **Database Validation: run the live installer once and read the artifacts back — PERMITTED 2026-09-20, and the substance is now done.** The local `mtm_waitlist` already exists with **23 tables** and the **seven prepared jobs**, so the paired-artifact round-trip and the read-path checks run against a live store rather than a seed. Verified today: **T175** (the read procedure returns `100-7`'s `part_number` plus its Die row `FGT0002000` / `DIE SHOP`) and **T185** (`100-3`'s only die row is the `No Die` placeholder, while `100-7` and `V100-33` carry real dies). **One limit that permission cannot lift:** the shipped installer is a **GUI-only** VBS, so a *full* reinstall still needs a person at the keyboard — the SQL artifacts were used directly instead. | **Persona: Database Engineer** — verified 2026-09-20: queries against the local store; evidence written into `tasks.md` T175 and T185
- [ ] **Verification: walk the app on screen for the UI gates this feature still owes.** **PERMITTED 2026-09-20 and the blocker is gone:** the built app reaches the **shell** against the local store with **no sign-in** — it auto-signs-in as `johnk` — so **T164 (dunnage step)** and **T174 (the die card reads the job)** can be walked by UI Automation. **T184 and T188 cannot**, and permission does not change that: they test the die step (T182) and the request page (T187), and neither exists yet. Walk them after those two land. (Ref: `specs/004-unified-card-item-picker/tasks.md`; `.github/instructions/winui3-ui-automation.instructions.md`) | **Persona: QA Engineer**
- [ ] **Verification: hold the 2026-10-12 SC-007 / SC-008 re-measurement — the real blocker is not the date (metric proven, result owed).** The windows opened 2026-09-12 and the re-check is due 2026-10-12, but 30 days of wall time would not have produced the number: found 2026-09-20, `RefreshRunRecordStore` and `BackupArtifactStore` each keep only the **latest** run per shape and per store, the only durable history is the prose JSONL log whose `RetentionDays` is a hard-coded **30** — the exact period being measured — and `BackupPolicy.RetentionCount` defaults to **14** artifacts per store. **What is now done instead:** the metric itself is proven against a seeded 30-day history by `tools/measure-reliability-window.ps1`, and both thresholds demonstrably bite — 93 % fails SC-007, a non-outage skip fails SC-007 even at 97 %, and one missing backup artifact fails SC-008. **What is still owed:** the real measurement, which needs a durable run history (`specs/004` T230) and then 30 days on the host. The seeded result proves the **metric**, never the service, and must be reported that way. (Ref: `specs/001-module-mock-visual-fallback/tasks.md` T106; `specs/004-unified-card-item-picker/tasks.md` T229–T232) | **Persona: QA Engineer**

Next task: **walk the app on screen for the UI gates this feature still owes** | **Persona: QA Engineer**

**GATE: the suite is green, the UI gates have a text dump behind them, and the live install has been re-read. A green build with an unwalked screen is a claim about code, not about the application.**

---

## Appendix: how each finding was verified

| Finding | Evidence |
| --- | --- |
| Build clean, suite green | `dotnet build` → `0 Warning(s) 0 Error(s)`; `dotnet test` → `Failed: 0, Passed: 1082, Skipped: 27, Total: 1109` (2026-09-20) |
| Suite is 20 tests larger than the recorded evidence | `specs/004/tasks.md` T179 records `total: 1089`; `WhatINeedToDo.md` repeats it |
| Duplicated table folders | Read `Database/Tables/02_core_workstations_registry/create.sql` (creates `core_computers_registry`) beside `02_core_computers_registry/create.sql` (identical object); same pattern at `13_*` and `14_*`; `AllTables.sql` lists each table once |
| Rollback-only folders are intentional | `Database/Tables/21_mock_parts/` and `28_waitlist_request_types/` contain `rollback.sql` only; the audit's documented exemptions cover them |
| SQL validator crash | Ran `.github/scripts/validate-sql-naming.ps1` → `Cannot overwrite variable Error…` at line 260, exit 1, no findings printed |
| 11 hidden SQL violations | Ran the same script with only the reserved-variable name changed; it printed the 11 findings quoted in Phase 4.1 |
| 901 C#/XAML findings | Ran `.github/scripts/validate-csharp-xaml-naming.ps1` over the tree; grouped by project: Tests 490, Strings 353, Mock.Service 18, Core 15, Settings 10, Waitlist.View 8, Setup 5, Mock 1, NewRequest 1 |
| Naming check is diff-scoped | Read `.github/workflows/csharp-xaml-naming-compliance.yml` — it builds a changed-file list from `git diff --name-only` |
| Retired-symbol gate is real | Read `MTM_Waitlist.Tests/Module_Mock/RetiredSymbolAuditTests.cs`, including `EveryRetiredPattern_BitesOnAReintroductionOfItsSymbol` |
| FR-054 not built | `DieIdentifier`, `NewRequestDieViewModel` and `NewRequestDiePage` return no matches anywhere in the tree; `NewRequestFlowRules` has a `dunnage` branch and no `die` branch |
| FR-048…FR-051 built | `RequestItemFieldDefinition.Lists` + `List`, the two dunnage config rows carrying `"list":"dunnage"`, `NewRequestFlowRules` routing on `DeclaredJobListName`, and the dunnage page/view model exist |
| Sorting claim in `FEATURES.md` is false | `UrgencyCalculator.OrderBy` defaults to most-urgent; `WaitlistViewViewModel.SortOrder` resolves the remembered key with `WaitlistSortOrder.MostUrgent` as the default |
| Retired catalog tables gone | Only `rollback.sql` files and the recorded retirement note mention `waitlist_request_types` / `_subtypes` |
| `capabilities/DRIFT.md` absent | Directory listing of `capabilities/` and a tree-wide search for `DRIFT.md` return nothing |
| Empty `specs/005` directory | Directory listing shows `.trace.jsonl` only; no `.spec-context.json` exists for it |
| Stale always-on guidance | `.github/instructions/database-schema-rules.instructions.md` lists `core_workstations_registry` and a "FluentMigrator runner" |
| The UI gates are reachable without a person | Verified 2026-09-20 by launching the built app against the local store with both connection-string environment variables set: it reached the **shell**, auto-signed-in as `johnk`, with the cached-data bar correctly reporting Infor Visual unreachable. No sign-in was required |
| The local store is seeded and live | `mtm_waitlist` holds **23 tables** and **7** `setup_active_jobs` rows — `100-3`, `100-6`, `100-7`, `100-18`, `100-1806`, `V100-33`, `V100-34`. `172.16.1.104` is unreachable from this machine, so `MySqlHostFallback` is what points the stores at localhost |
| The die rule holds on live data | Read through `JSON_TABLE(setup_active_jobs.subordinate_parts_json)`: `100-3` carries `FGT0001-01` described **`No Die`** with an empty Location (the placeholder FR-055 refuses), `100-7` carries `FGT0002000` / `DIE SHOP`, `V100-33` carries `FGT0002001` / `PRESS BAY` |
| The die read path works | `sp_setup_active_jobs_latest_by_work_center_get` takes **no arguments** and returned **8** rows; `100-7`'s carries `part_number = PART-9003`, `sequence_number = 10` and its Die row. (The task text implied a per-work-centre call — corrected.) |
| Duplication extends to procedures | **68** live procedures against **109** folders on disk, and **6** of the absent ones still hold a `create.sql`: `sp_setup_workstations_delete` / `_get_all` / `_touch` / `_upsert` and `sp_config_hot_workcenters_delete_for_workstation` / `_get_for_workstation`. `sp_setup_workstations_get_all` is **byte-identical** to `sp_setup_work_centers_get_all`, and `sp_setup_workstations_upsert` to `sp_setup_work_centers_upsert` — the stale folder creates the renamed procedure. The other 35 absent folders are rollback-only retired objects and are correct |
| The shipped installer is GUI-only | `Database/install_local_database.bat` runs `install_local_database.vbs` through `wscript.exe` because it uses InputBox/MsgBox dialogs, so it cannot be driven unattended — a full reinstall still needs a person at the keyboard |
| T180 answered | Owner decision 2026-09-20: a die goes to its home location and the person is never asked where it goes. Written into `spec.md`, `research.md` D22, `data-model.md`, the three contracts, the CSV's two die rows and `tasks.md` Phase 13; no schema change follows from it |
| D-2 answered | Owner decision 2026-09-20: both die Items ask which die, the step offers select-all, and the request page shows every die location. Recorded as `spec.md` FR-054 and the new FR-057, the flow contract's die rows, and `tasks.md` T182 and T187 |
| D-3 answered | Owner decision 2026-09-20: validate each pair is identical, delete the extras, then validate no procedure, view, function or aggregate points at a removed folder — and treat duplicated procedures the same way. Carried as the five tasks in Phase 3.1 |
| D-4 and D-7 answered | Owner decision 2026-09-20: retire the no-grandfather rule and record the violations in a report instead of normalising them now — 901 C#/XAML findings and 11 SQL findings. Carried as the report tasks in Phase 4.1 and 4.2 |
| Phases 2–4 re-homed | 2026-09-20: mirrored into `specs/004-unified-card-item-picker/tasks.md` **Phase 14, T189–T228** so `/speckit.ralph.run` can drive them; the three owner-only actions were left out on purpose. The file's open count moved from 18 to **58** |
| Owner permission granted | 2026-09-20: the agent may do the database round-trip and the in-app UI gates. Established by running it — the live round-trip is **doable** against the local store, the UI gates are **reachable** by UI Automation, and T184/T188 remain blocked because the code they test (T182/T187) does not exist. The 2026-10-12 measurement stays impossible |
| T175 and T185 verified | 2026-09-20: the die read path and the FR-055 split confirmed against the live local store; both tasks ticked with their evidence in `tasks.md` |
| SC-007/SC-008 were unmeasurable, not merely unmeasured | Read the service's own bookkeeping 2026-09-20: `RefreshRunRecordStore._lastRuns` and `BackupArtifactStore._lastRuns` are dictionaries keyed by shape and by store, so each holds **one** run and no history; `ServiceLog.RetentionDays` is a hard-coded `30` (`Services/ServiceLog.cs:39`) and `PruneAsync`/`RunRetentionSweep` delete whole days past it; `BackupPolicy.RetentionCount` defaults to `14` (`Models/BackupPolicy.cs:20`). Every retention is shorter than or equal to the 30-day window the criteria measure |
| The SC-007/SC-008 metric is now proven, on seeded data | Ran `tools/measure-reliability-window.ps1` over four seeded 30-day histories: `healthy` 29/30 = 97 % → SC-007 PASS and 0/30 windows missing → SC-008 PASS; `marginal` 28/30 = 93 % → SC-007 FAIL; `unattributed` 97 % with one `schemaMismatch` skip → SC-007 FAIL (attribution rule bites independently of the rate); `broken` 100 % refresh with one store's artifact absent → SC-008 FAIL. A fifth run measured the 30-interval history against a 45-interval expectation → **SC-007 FAIL** with `MissingIntervals 15` and a 100 % success rate, proving an interval that never ran cannot pass. The seeded history is synthetic, so this proves the gate and not the service |
