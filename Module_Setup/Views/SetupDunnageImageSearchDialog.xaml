<ContentDialog
    x:Class="MTM_Waitlist.Module_Setup.Views.SetupDunnageImageSearchDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:MTM_Waitlist.Module_Setup.Converters"
    xmlns:models="using:MTM_Waitlist.Module_Setup.Models"
    Title="Search Dunnage Parts by Image"
    CloseButtonText="Cancel"
    DefaultButton="Close"
    Opened="OnOpened">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">920</x:Double>
        <x:Double x:Key="ContentDialogThemeMaxWidth">920</x:Double>
        <x:Double x:Key="ContentDialogThemeMinWidth">920</x:Double>
        <x:Double x:Key="ContentDialogMinWidth">920</x:Double>
        <x:Double x:Key="ContentDialogMinHeight">560</x:Double>
        <x:Double x:Key="ContentDialogThemeMinHeight">560</x:Double>
        <x:Double x:Key="ContentDialogThemeMaxHeight">640</x:Double>
        <x:Double x:Key="ContentDialogMaxHeight">640</x:Double>
        <x:Double x:Key="ContentDialogHeight">640</x:Double>
        <x:Double x:Key="ContentDialogThemeHeight">640</x:Double>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:SetupImagePathToImageSourceConverter x:Key="SetupImagePathToImageSourceConverter" />
    </ContentDialog.Resources>

    <Grid HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Padding="16"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox
                    Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    PlaceholderText="Filter by part, type, or location"
                    Text="{x:Bind ViewModel.FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <Button
                    Grid.Column="1"
                    Margin="8,0,0,0"
                    Command="{x:Bind ViewModel.LoadPartsCommand}"
                    Content="Refresh" />
                <ToggleSwitch
                    Grid.Column="2"
                    Margin="12,0,0,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    IsOn="{x:Bind ViewModel.ShowPartsWithoutImages, Mode=TwoWay}"
                    OffContent="Images only"
                    OnContent="Show all"
                    ToolTipService.ToolTip="Include parts that do not have an image"
                    Toggled="OnShowPartsWithoutImagesToggled" />
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,16,0,0">
            <GridView
                x:Name="PartGridView"
                HorizontalAlignment="Stretch"
                IsItemClickEnabled="True"
                ItemClick="OnPartClick"
                ItemsSource="{x:Bind ViewModel.DisplayedParts, Mode=OneWay}"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                ScrollViewer.HorizontalScrollMode="Disabled"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollMode="Auto"
                SelectionMode="None">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="VerticalContentAlignment" Value="Stretch" />
                    </Style>
                </GridView.ItemContainerStyle>
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="models:SetupDunnagePart">
                        <Border
                            Margin="4"
                            Padding="12"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <Border
                                    Grid.Row="0"
                                    Width="96"
                                    Height="96"
                                    Margin="0,0,0,10"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8">
                                    <Grid>
                                        <Image
                                            Source="{x:Bind ImagePath, Converter={StaticResource SetupImagePathToImageSourceConverter}}"
                                            Stretch="Uniform"
                                            Visibility="{x:Bind HasImage, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <FontIcon
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            FontSize="32"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Glyph="&#xE710;"
                                            Visibility="{x:Bind ShowNoImagePlaceholder, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    </Grid>
                                </Border>

                                <TextBlock
                                    Grid.Row="1"
                                    HorizontalAlignment="Center"
                                    MaxLines="1"
                                    Style="{StaticResource BodyStrongTextBlockStyle}"
                                    Text="{x:Bind PartNumber}"
                                    ToolTipService.ToolTip="{x:Bind PartNumber}" />
                                <TextBlock
                                    Grid.Row="2"
                                    Margin="0,4,0,0"
                                    HorizontalAlignment="Center"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="{x:Bind DunnageTypeName}"
                                    ToolTipService.ToolTip="{x:Bind DunnageTypeName}"
                                    TextAlignment="Center"
                                    TextWrapping="Wrap" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>

            <Grid
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Visibility="{x:Bind ViewModel.HasNoResults, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Border
                    MaxWidth="420"
                    Padding="16,10"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <TextBlock
                        TextAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource BodyTextBlockStyle}"
                        Text="{x:Bind ViewModel.EmptyStateMessage, Mode=OneWay}"
                        TextWrapping="Wrap" />
                </Border>
            </Grid>
        </Grid>
    </Grid>
</ContentDialog>
