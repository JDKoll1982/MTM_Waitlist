<ContentDialog
    x:Class="MTM_Waitlist.Module_Waitlist.Views.NewRequestJobTypeDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Name="RootDialog"
    DefaultButton="Close"
    CloseButtonText="Cancel"
    Title="New Request - Choose Job Type">

    <Grid Width="560" MinHeight="360" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Name="WorkCenterTextBlock"
            Grid.Row="0"
            Style="{ThemeResource BodyStrongTextBlockStyle}"
            TextWrapping="WrapWholeWords" />

        <TextBlock
            Grid.Row="1"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Text="Choose a Job Type to continue."
            TextWrapping="WrapWholeWords" />

        <GridView
            x:Name="JobTypeGridView"
            Grid.Row="2"
            IsItemClickEnabled="True"
            ItemClick="JobTypeGridView_ItemClick"
            SelectionMode="Single">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Border
                        MinWidth="220"
                        Margin="8"
                        Padding="12"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="10">
                        <Grid RowSpacing="6">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock
                                Grid.Row="0"
                                Style="{ThemeResource BodyStrongTextBlockStyle}"
                                Text="{Binding RequestType}"
                                TextWrapping="WrapWholeWords" />

                            <TextBlock
                                Grid.Row="1"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="{Binding SubtypeSummary}"
                                TextWrapping="WrapWholeWords" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</ContentDialog>
