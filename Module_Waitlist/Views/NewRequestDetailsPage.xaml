<Page
    x:Class="MTM_Waitlist.Module_Waitlist.Views.NewRequestDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:MTM_Waitlist.Module_Settings.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid Style="{StaticResource SetupPageGridStyle}" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Style="{StaticResource SetupPageHeaderStyle}">
            <TextBlock Style="{StaticResource SetupPageTitleStyle}" Text="Additional details" />
            <TextBlock Style="{StaticResource SetupPageSubtitleStyle}" Text="{x:Bind ViewModel.Heading, Mode=OneWay}" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource SetupSectionCardStyle}">
            <StackPanel Spacing="10">
                <TextBox
                    Header="{x:Bind ViewModel.PromptText, Mode=OneWay}"
                    Text="{x:Bind ViewModel.InputValue, Mode=TwoWay}"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    PlaceholderText="Enter a short description"
                    MinWidth="360"
                    MaxWidth="520"
                    HorizontalAlignment="Left" />
                <TextBlock
                    Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                    Text="{x:Bind ViewModel.ValidationMessage, Mode=OneWay}"
                    TextWrapping="WrapWholeWords"
                    Visibility="{x:Bind ViewModel.IsValidationVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" />

        <StackPanel Grid.Row="3" Style="{StaticResource SetupActionBarStyle}">
            <Button
                Content="Back"
                Command="{x:Bind ViewModel.BackCommand}" />
            <Button
                Content="Continue"
                Command="{x:Bind ViewModel.ContinueCommand}"
                Style="{StaticResource AccentButtonStyle}" />
        </StackPanel>
    </Grid>
</Page>
