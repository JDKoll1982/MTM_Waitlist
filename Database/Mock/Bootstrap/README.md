# Database/Mock/Bootstrap

Bootstrap artifacts for the `mtm_mock` cache database.

- `create_database.sql` — re-runnable creation of the `mtm_mock` database (task T007)
- `rollback.sql` — drops the database (task T007)
- `update_table_descriptions.sql` — mandatory table/column descriptions (task T008)
