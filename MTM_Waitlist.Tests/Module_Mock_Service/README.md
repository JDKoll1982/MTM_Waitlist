# MTM_Waitlist.Tests/Module_Mock_Service

Tests for the `MTM_Waitlist.Mock.Service` on-host service (refresh engine swap semantics,
backup/restore guards, and the token-gated HTTP API). Pure-unit only — no live host required.
